# 分词器训练流程解析（train_tokenizer.py）

> 从语料中学习一套**文本 → token id** 的映射规则（词表），是所有训练的前置步骤。
>
> **注意**：MiniMind 已自带分词器，此脚本仅供学习参考。

---

## 一、什么是分词器？

分词器（Tokenizer）是模型和人类语言之间的"翻译官"：

```
文本:      "今天天气真好"
           ↓ 分词器编码
token ids: [2156, 1034, 892, 5567]
           ↓ 模型处理
logits:    [...]
           ↓ 分词器解码
文本:      "明天也不错"
```

模型不认识文字，只能处理整数序列。分词器定义了"哪些字/词对应哪个整数"。

---

## 二、与其他训练脚本完全不同

这个脚本**不涉及模型训练**，不使用 PyTorch 的训练循环、优化器、GPU 等。它是一个纯 CPU 的文本处理任务。

| 维度 | 其他训练脚本 | 分词器训练 |
|------|------------|-----------|
| 框架 | PyTorch | **HuggingFace tokenizers** |
| 计算设备 | GPU | **CPU** |
| 输入 | token 序列 | **原始文本** |
| 输出 | 模型权重 | **词表文件（tokenizer.json + config）** |
| 训练循环 | forward/backward/step | **BPE 统计合并** |

---

## 三、训练流程

### 3.1 数据准备

```python
DATA_PATH = '../dataset/pretrain_hq.jsonl'
VOCAB_SIZE = 6400  # 词表大小

def get_texts(data_path):
    with open(data_path, 'r', encoding='utf-8') as f:
        for i, line in enumerate(f):
            if i >= 10000: break  # 实验性，只用前 10000 行
            data = json.loads(line)
            yield data['text']
```

### 3.2 BPE 算法训练

```python
def train_tokenizer(data_path, tokenizer_dir, vocab_size):
    # 1. 创建 BPE 分词器
    tokenizer = Tokenizer(models.BPE())

    # 2. 设置字节级预分词（将文本先拆成 UTF-8 字节序列）
    tokenizer.pre_tokenizer = pre_tokenizers.ByteLevel(add_prefix_space=False)

    # 3. 配置训练器
    trainer = trainers.BpeTrainer(
        vocab_size=vocab_size,                         # 词表大小 6400
        special_tokens=["<|endoftext|>",               # PAD/UNK (id=0)
                        "<|im_start|>",                # BOS/消息开始 (id=1)
                        "<|im_end|>"],                 # EOS/消息结束 (id=2)
        initial_alphabet=pre_tokenizers.ByteLevel.alphabet()
    )

    # 4. 训练！从文本迭代器中学习合并规则
    texts = get_texts(data_path)
    tokenizer.train_from_iterator(texts, trainer=trainer)

    # 5. 设置解码器
    tokenizer.decoder = decoders.ByteLevel()
```

**BPE（Byte-Pair Encoding）算法的直觉：**

```
初始：每个字节是一个 token
  "今天" → [0xE4, 0xBB, 0x8A, 0xE5, 0xA4, 0xA9]  （6 个字节/token）

第 1 轮合并：统计最频繁的字节对 → (0xE4, 0xBB) 最多 → 合并为新 token
第 2 轮合并：再找最频繁的对 → 继续合并
...
第 N 轮：直到词表达到 6400

最终："今天" 可能变成 [2156, 1034]（2 个 token）甚至 [3021]（1 个 token）
```

> 高频词/字会被合并成单个 token，低频词则保持为多个子词/字节。这就是为什么"中文 1 token ≈ 1.5~1.7 字符"。

### 3.3 保存词表和配置

```python
# 保存分词器
tokenizer.save(os.path.join(tokenizer_dir, "tokenizer.json"))

# 保存配置（定义特殊 token、chat_template 等）
config = {
    "bos_token": "<|im_start|>",
    "eos_token": "<|im_end|>",
    "pad_token": "<|endoftext|>",
    "chat_template": "...(Jinja2 模板)...",
    ...
}
with open(os.path.join(tokenizer_dir, "tokenizer_config.json"), "w") as f:
    json.dump(config, f)
```

### 3.4 验证分词器

```python
def eval_tokenizer(tokenizer_dir):
    tokenizer = AutoTokenizer.from_pretrained(tokenizer_dir)

    # 测试 chat_template
    messages = [
        {"role": "system", "content": "你是一个优秀的聊天机器人"},
        {"role": "user", "content": "你来自哪里？"},
        {"role": "assistant", "content": "我来自地球"}
    ]
    prompt = tokenizer.apply_chat_template(messages, tokenize=False)

    # 测试编码/解码一致性
    input_ids = tokenizer(prompt).input_ids
    decoded = tokenizer.decode(input_ids, skip_special_tokens=False)
    assert decoded == prompt  # 编码后再解码应该完全一样

    # 测试流式解码（逐 token 解码，模拟推理时的流式输出）
    for tid in input_ids:
        token = tokenizer.decode([tid])
        print(f'Token ID: {tid} -> {token}')
```

---

## 四、输出文件

| 文件 | 用途 |
|------|------|
| `tokenizer.json` | 词表 + 合并规则（分词器的核心） |
| `tokenizer_config.json` | 特殊 token 定义 + chat_template |
| `merges.txt` | BPE 合并规则（可选） |
| `vocab.json` | 词表映射（可选） |

---

## 五、关键概念

| 概念 | 说明 |
|------|------|
| `vocab_size = 6400` | 词表大小，MiniMind 使用 6400（极小，GPT-4 用 ~100k） |
| `ByteLevel` | 字节级分词，任何 UTF-8 文本都能编码，不会出现 UNK |
| `BPE` | 从高频字节对不断合并，自适应构建子词词表 |
| `special_tokens` | `<\|endoftext\|>`(PAD)、`<\|im_start\|>`(BOS)、`<\|im_end\|>`(EOS) |
| `chat_template` | ChatML 格式的 Jinja2 模板，定义对话如何格式化 |
